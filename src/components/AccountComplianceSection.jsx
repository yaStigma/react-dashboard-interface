export default function AccountComplianceSection() {
  // if (!workQueue || workQueue.length === 0) {
  //   return <div>No data available</div>;
  // }

  return (
    <section className="w-full h-full flex flex-row items-start ">
      <h2 className="text-subheading font-subheading leading-subheading text-textMuted mb-5 ">
        Compliance & Documentation
      </h2>
      <div>
        <div className="w-[673px] h-[159px] flex flex-col items-start py-[28px] px-[40px]   rounded-[20px] border  border-cardBorder bg-cardLight"></div>
      </div>
    </section>
  );
}
